#include<stdio.h>
int main(){
	int i,n,f=1;
	printf("enter a number:");
	scanf("%d",&n);
	if(n<0){
		printf("Factorial is not defined for negative numebrs:");
	}
	else{
	for(i=1;i<=n;++i){
		f=f*i;
	}
	printf("Factorial of %d is %d:",n,f);
}
}
